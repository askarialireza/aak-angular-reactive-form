import { FormGroup } from '@angular/forms';
import { BaseFieldItem } from './../interfaces/field-item/base-field-item.interface';

export class AAkFormGroup extends FormGroup {

  constructor(BaseFieldItem[]) {
    super({});
  }
}
